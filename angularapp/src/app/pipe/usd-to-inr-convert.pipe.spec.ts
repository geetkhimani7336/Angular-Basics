import { UsdToInrConvertPipe } from './usd-to-inr-convert.pipe';

describe('UsdToInrConvertPipe', () => {
  it('create an instance', () => {
    const pipe = new UsdToInrConvertPipe();
    expect(pipe).toBeTruthy();
  });
});
